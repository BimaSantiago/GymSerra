import SimpleParallax from "../layout/SimpleParallax";

const Principal = () => {
  return (
    <section id="principal" className="relative h-full">
      <SimpleParallax />
    </section>
  );
};

export default Principal;
